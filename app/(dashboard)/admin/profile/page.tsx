import ProfilePageContent from "@/components/admin/ProfilePageContent";

export const metadata = {
  title: "Profile — Slughub",
};

export default function ProfilePage() {
  return <ProfilePageContent />;
}
